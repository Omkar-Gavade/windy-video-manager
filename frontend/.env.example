# Base URL of the FastAPI backend.
# In the Docker deployment this is set to "/" at build time so nginx proxies
# /api to the backend (same origin). Only needed for local `npm run dev`.
VITE_API_BASE_URL=http://localhost:8000
