# Base URL of the FastAPI backend.
VITE_API_BASE_URL=http://localhost:8000

# Client-side upload size hint (MB). Should match the backend MAX_UPLOAD_MB.
# The backend remains the authoritative limit.
VITE_MAX_UPLOAD_MB=200
